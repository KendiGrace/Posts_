package com.example.posts.Models

//creating a data class that holds data about the Post we want displayed.
data class Post(
    var userId:Int,
    var Id:Int,
    var title:String,
    var body:String
)
